// Los mapas se dibujan a mano en un canvas a partir del mismo tile map que
// usa el tablero — no hay nada que el servidor pueda renderizar, y
// `document` tiene que existir para que el canvas funcione.
export const ssr = false;
export const prerender = false;
