// Los retratos son un canvas pintado en tiempo real por el mismo motor de
// sprites del tablero — no hay nada que el servidor pueda renderizar, y
// `document` tiene que existir para que cualquiera de ellos funcione.
export const ssr = false;
export const prerender = false;
